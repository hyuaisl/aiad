# PyTorch는 scripts/setup.py가 CUDA / MPS / CPU 환경에 맞춰 별도로 설치합니다.
# CPython 3.12, 64-bit. Install setuptools/wheel/swig before this file.
# Windows ARM64 needs NumPy 2 and the official native PyTorch CPU wheel.
numpy==2.3.5; sys_platform == 'win32' and platform_machine == 'ARM64'
numpy==1.26.4; sys_platform != 'win32' or platform_machine != 'ARM64'
gymnasium==1.3.0
pygame-ce==2.5.8
PyYAML==6.0.3
# Box2D 2.3.10 has no Windows/Linux ARM64 wheels or source release.
# Both packages expose the same Box2D module; never install both together.
box2d-py==2.3.8; (sys_platform == 'win32' and platform_machine == 'ARM64') or (sys_platform == 'linux' and platform_machine == 'aarch64')
Box2D==2.3.10; (sys_platform != 'win32' or platform_machine != 'ARM64') and (sys_platform != 'linux' or platform_machine != 'aarch64')
